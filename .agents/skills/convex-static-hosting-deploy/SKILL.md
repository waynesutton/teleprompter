---
name: convex-static-hosting-deploy
description: Router for legacy static-hosting deploy references. Prefer deploydev for /deploydev and deployprod for /deployprod.
---

# Convex Static Hosting Deploy Router

This legacy combined skill has been split into two target-specific skills:

- `/deploydev` -> `.agents/skills/deploydev/SKILL.md`
- `/deployprod` -> `.agents/skills/deployprod/SKILL.md`

Use the split skill directly so dev and production deploys stay separate.
